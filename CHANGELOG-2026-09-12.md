# 這次改了什麼（2026-09-12）

針對你列的 7 點回饋，逐項說明改了什麼、為什麼、還有哪些要你自己確認一下。

## ⚠️ 上傳前務必看：一個很關鍵的設定錯誤

`_config.yml` 裡的 `baseurl` 原本還留著 `"/en-chiang-site-v2"`（測試子路徑用的設定），
但 `enchiang.com` 現在已經是正式網域直接指到這個 repo，`baseurl` 應該要是空字串 `""`。

**這個錯誤的直接後果：只要照片存的是「站內相對路徑」（例如 `/images/xxx.jpg`），
網址就會被多加一段變成 `enchiang.com/en-chiang-site-v2/images/xxx.jpg`，實際上不存在，就會破圖。**
用完整網址存的照片（`https://enchiang.com/images/xxx.jpg`）不受影響，因為完整網址本來就不會被加前綴。

這次已經改回空字串。如果你看目前正式站首頁的圖是正常的，代表這個錯誤可能已經在別的地方修過了；
但你上傳這批檔案時**請再檢查一次 `_config.yml` 裡的 `baseurl` 是不是空字串**，不要不小心蓋回舊的設定。

---

## 1. Minibar：點主項目自動展開文章＋相簿，相簿字級變小

- 「關於EN／關於日常／關於愛」三個主項目，原本點下去會直接跳去第一篇文章；現在改成**點主項目本身（或旁邊的箭頭）就會往下展開**，同時顯示這個分類底下的文章清單跟相簿清單，不會再跳頁面。
- 文字大小重新拉出層級：文章標題 14.5px（用襯線字），相簿名稱縮小到 11px、前面加一個小小的「—」記號，跟文章明顯有主副之分，比較接近 V1 的層級感。
- 「關於商業」維持原本邏輯不變：點主項目文字還是直接連到 `/business/`（因為這個分類本來就有自己的真實頁面），箭頭則是另外展開四個子分類清單。

改的檔案：`_includes/nav.html`、`_layouts/default.html`（CSS + `toggleMenuGroup()` 這個 JS 函式）。

## 2. ＋5. 關於商業「無法一次全部展示」＋破圖排查

排查後找到兩個問題，都已修正：

**(a) 相簿變成跑馬燈，一次只能看一張。** 「創意平面」跟「影像」兩個分類的相簿，原本用的是「一次顯示一張、要點左右箭頭切換」的跑馬燈效果，這跟你要的「全部展示」剛好相反。已經改回**照片直接排成一整排網格、全部同時顯示**（跟文章內頁的相簿排版方式一致）。

**(b) 用瀏覽器實際打開 `enchiang.com/business/` 檢查，發現頁面卡在「內容載入中……」，圖完全沒有跑出來。** 這代表**目前正式站上的 `/business/` 頁面，很可能還是改版前的舊版本**（舊版是用 JavaScript 動態去抓 `index.html` 內容來顯示，改版後 `index.html` 的結構已經整個換掉，舊的抓取邏輯找不到它要的內容，才會一直卡在「載入中」）。

這部分我這邊沒有 repo 存取權限，沒辦法直接確認 GitHub 那邊實際部署的檔案內容，所以麻煩你上傳這批新檔案之後，做兩件事確認：

1. 到 repo 的 **Actions** 分頁，看最新一次的建置（build）是不是綠勾勾成功、還是紅叉叉失敗。如果失敗，GitHub Pages 會**繼續顯示上一次成功的舊版本**，不會有任何警告畫面，這是最容易被忽略的地方。
2. 確認 repo 根目錄現在確實是這次給你的新版 `business.html`（有 `permalink: /business/` 那個版本），而不是另一份同名但邏輯不同的舊檔案。

改的檔案：`business.html`、`_layouts/default.html`（拿掉跑馬燈 CSS/JS 依賴，改用既有的 `.album-photo-grid` 排版）。

## 3. 後台設定更直觀，減少複雜分層

做了兩件事：

**(a) 右側預覽畫面現在長得跟前台一樣。** 之前你截圖那種「一排欄位名稱：內容」的純文字列表，是 Decap CMS 沒有客製化預覽時的預設畫面。這次新增了 `admin/preview.js`，幫首頁、關於EN/日常/愛的文章、關於商業四個分類都各自寫了一個「用前台真正的排版跟樣式」的預覽元件——編輯的時候右邊會直接看到接近真實頁面的樣子，不用再自己腦補欄位對應到哪裡。

同時把原本寫死在 `_layouts/default.html` 裡面的一大段 `<style>`，抽成獨立的 `assets/main.css`，前台頁面跟後台預覽共用同一份樣式，以後改版型只要改一個地方。

**(b) 長列表預設收合，用標題當摘要。** 相簿、照片、分類、影片這幾種常常會很多筆的清單，現在預設是「收合」狀態，清單上直接顯示標題（例如相簿名稱、照片說明），不用每一筆都展開才看得到內容，往下拉的量會少很多。

改的檔案：`admin/config.yml`（加 `collapsed: true` + `summary`）、新增 `admin/preview.js`、新增 `assets/main.css`（從 `_layouts/default.html` 抽出來）、`admin/index.html`（多載入一行 `preview.js`）。

## 4. 新增「整批上傳照片（zip）」＋讓自動解壓縮真的能動

發現一個之前留下的坑：`incoming-zips/README.md` 已經寫好「丟一個 zip 進來，GitHub Actions 會自動解壓縮」的說明，**但負責做這件事的 GitHub Actions 設定檔其實從來沒有真的建立過**（`.github/workflows/` 資料夾原本是空的）。也就是說，這個功能之前只有說明文件，沒有真正運作。

這次補上：

- `.github/workflows/incoming-zips.yml`：只要 `incoming-zips/` 資料夾有變動（不管是你手動丟 zip，還是透過後台上傳），就會自動觸發。
- `scripts/process-incoming-zips.js`：實際解壓縮、把照片拉平搬進 `images/`、檔名重複就自動加時間戳記、處理完刪除 zip。
- 後台新增一個「📦 整批上傳照片（zip，自動解壓縮）」的區塊，可以直接在 `/admin/` 上傳 zip，不用另外用 GitHub 網頁操作。

**上傳完之後**，等 1～2 分鐘讓 Actions 跑完（可以到 Actions 分頁看綠勾勾），這批照片就會進到 `images/` 資料夾。之後在任何一個相簿的照片欄位，直接點「Choose an image」，就能從縮圖清單裡挑剛剛上傳的照片，不用再自己去 `images/images-README.md` 找檔名、複製連結。

⚠️ 這個功能需要 repo 的 Actions 有「寫回 repo」的權限，才能自動 commit 處理結果。麻煩去 **repo 的 Settings → Actions → General → Workflow permissions**，確認選的是「**Read and write permissions**」，不然這個自動化會執行但存不回去。

## 6. AI自動化新增社群圖示（IG／YT／TikTok／FB／Threads）

「孝子貓」相簿的「立即查看」旁邊，現在可以個別設定最多 5 個平台的圖示按鈕，每個平台的連結都可以自己填、留空就不會顯示那顆圖示。已經把原本藏在照片說明文字裡的 YouTube／Instagram／TikTok 連結，搬到新的「社群圖示」欄位裡，Facebook 跟 Threads 先留空給你之後自己填。

改的檔案：`_data/business/ai-automation.yml`、`admin/config.yml`（新增 `social_links` 欄位）、`business.html`（畫出圖示）、`_layouts/default.html`（圖示的 CSS）。

## 7. 首頁「最新消息」改成「關於商業」精選

原本這區的資料結構其實一直都可以自己選圖片跟文字（`recent_shoots.items`），但區塊標籤是「What's NEW／最新消息」，內容也一直放的是生活隨拍（貓咪、樂團演出），跟「商業」無關。

這次把它重新定位成**首頁的「關於商業」精選區**：

- 小標籤／標題改成 `Business` / `關於商業`
- 新增「按鈕文字」「按鈕連結」，預設連到 `/business/`，區塊下方會有一個按鈕
- 每張精選照片新增「連結」欄位，可以自己決定點下去要連到哪個商業分類（例如 `/business/#biz-brand`）
- 先放了 3 張示範用的商業照片（個人形象／燒肉天皇／昕澄診所）卡位，**這只是示範，實際要放什麼照片、寫什麼文字，到後台「首頁內容」→「首頁商業精選」自己換掉即可**

改的檔案：`_data/site.yml`、`admin/config.yml`、`index.html`。

---

## 這次沒做、但你應該知道的事

- 「整批上傳」目前**只做「解壓縮＋歸位＋防止覆蓋」**，沒有加自動壓縮圖片（舊系統的 `compress-images.js` 那種 JPEG/PNG 壓縮）。如果覺得檔案太大想加，之後可以再處理，這次先聚焦在「不用手動複製連結」這件事。
- 業務頁破圖／載入不出來的**根本原因**（第 2、5 點），高機率是「正式站現在還在跑改版前的舊 `business.html`」，但我這邊沒有這個 repo 的實際存取權限，沒辦法直接進去確認 GitHub 上目前部署的檔案內容，所以這部分麻煩你上傳後照上面「Actions 分頁確認建置成功」的步驟自己驗證一次，如果驗證後照片還是有問題，把畫面截圖給我，我可以照畫面繼續往下排查。

---

# 第二輪修復（同一天，針對你回報的 4 點）

## 1. 測試網站首頁壞了 — 找到最可能的原因，需要你確認一件事

你這次貼的是 `https://enchiang99.github.io/en-chiang-site-v2/` 這個「子路徑」測試網址。這種網址底下，`_config.yml` 的 `baseurl` 必須是 `"/en-chiang-site-v2"`，全站連結、圖片、CSS 才會自動補上這一段路徑；但上一輪為了修正正式站 `enchiang.com`（自己的網域，接在根目錄），把 `baseurl` 改成了空字串 `""`。**這兩種情境的設定互斥，同一份 `_config.yml` 沒辦法同時讓兩個網址都正常**——這極可能就是「上次修好正式站、這次子路徑測試站卻壞掉」的原因。

這次已經把 `baseurl` 改回 `"/en-chiang-site-v2"`，先讓你貼的這個測試網址恢復正常。但麻煩你確認一件事：**`https://enchiang99.github.io/en-chiang-site-v2/` 跟 `enchiang.com`，是不是同一個 repo？**

- 如果是同一個 repo（只是一個有接自訂網域、一個沒有）：正式的驗證方式應該是直接看 `enchiang.com`，不要再用 github.io 那個子路徑網址，因為兩者沒辦法同時用同一份設定正常顯示。
- 如果這是兩個不同的 repo（一個正式、一個測試）：那完全沒問題，各自維護各自的 `baseurl` 就好，這次的檔案是照「子路徑測試」設定的，上傳到測試 repo 沒問題；但如果你之後要把這批檔案搬去正式 repo，記得把 `baseurl` 改回空字串 `""`（`_config.yml` 裡有寫清楚）。

如果改完 `baseurl` 之後，這個測試網址還是有問題，麻煩截圖給我（尤其是完全空白、還是有內容但排版跑掉、還是某個地方明顯壞圖），我這邊沒辦法直接用瀏覽器看畫面，用截圖排查會快很多。

## 2. Hero 大圖後台無法顯示

把 `_data/site.yml` 裡 hero 圖片的路徑，從站內相對路徑 `/images/hero.jpeg` 改成完整網址 `https://enchiang.com/images/hero.jpeg`。

原本用相對路徑時，後台縮圖是靠 Decap CMS 去這個 repo 的 `images/` 資料夾裡找同名檔案來顯示縮圖——如果這個 repo 裡實際上沒有這個檔案（例如檔案其實只存在另一個 repo，或還沒上傳過），縮圖就會一直是空的。改成完整網址後，後台縮圖跟前台都直接用這個網址載入，不再依賴「這個 repo 裡有沒有這個檔案」。

如果改完後台縮圖還是顯示不出來，代表這條網址本身也有問題（不太可能，因為正式站首頁的大圖目前用的就是這條網址），到時候再截圖給我看。

## 3. 首頁「引言描述」跟服務卡描述，改成整塊文字框

原本「引言（關於EN）」下面的描述，跟三張服務卡各自的描述，都是「一行一格」的清單（每多一句話就要按一次「Add」）。這次改成 Decap CMS 的多行文字框（`widget: text`），可以直接貼一整段、用 Enter 換行分段，存檔後前台會自動照換行位置切成一段一段顯示，效果跟原本一樣，只是編輯體驗變成一次看到完整內容。

改的檔案：`admin/config.yml`（`quote.lines` → `quote.desc`，`desc_lines` → `desc_text`，widget 從 list 改成 text）、`_data/site.yml`（資料格式跟著改）、`index.html`（改用 `split: "\n"` 重新切段）、`admin/preview.js`（預覽畫面的切段邏輯也跟著更新）。

## 4. 關於商業：移除不會用到、容易混淆的欄位

- 各分類的「代碼」欄位（例如 `sub-1787241879912` 這種）：後台不會再顯示、也不能編輯了，但**沒有刪掉底層資料**——這個欄位改成 Decap CMS 的「隱藏欄位」（`widget: hidden`），資料還在，只是編輯畫面上看不到，不會有「以為填了但其實沒存到」的困惑，也不影響既有的分類錨點連結。
- 影音分類底下每支影片原本還有一個「子分類名稱」欄位，實際檢查後發現**前台完全沒有用到這個欄位**（純粹是殘留設定），這次直接整個移除，資料檔裡對應的內容也一併清掉了。

改的檔案：`admin/config.yml`（3 處「代碼」欄位改成 hidden，移除「子分類名稱」欄位）、`_data/business/video.yml`（清掉沒用到的 `subcategory` 資料）。

---

# 第三輪修復（同一天，針對你回報的 5 點）

## 1. 關於商業字體跟創意平面對齊

「影音 Video」卡片下方的說明文字，原本用的是無襯線字（跟創意平面/影像相簿標題的襯線字不一致）。已經改成跟相簿標題同一組字體（Noto Serif TC 襯線字），字級也從 12.5px 調整到 14px，視覺上跟創意平面的排版對齊。

改的檔案：`assets/main.css`（`.video-card-meta`）。

## 2. 影像／創意平面：兩格一列＋手動輪播，一律從第一張開始

這一點是把上一輪「全部展開成整排網格」的做法，改成你這次講清楚的版本：**相簿本身兩格一列排版，但每個相簿裡面的照片是手動輪播（不是全部攤開），而且輪播一律從第一張照片開始，不會自動跳頁**。

具體做的事：

- 「創意平面」「影像 Photography」兩個分類的相簿，改回輪播元件（`.album-carousel`），但這次**沒有加自動播放的設定**，所以永遠停在第一張，只有點左右箭頭或下方小圓點才會換頁。
- 找到並修好一個輪播元件本身的小 bug：共用的輪播 JS 之前即使沒有加「自動播放」的設定，也會用預設的 4.5 秒自動跳頁；這次修正成**沒有設定自動播放，就真的完全不會自動跳**，確保「打開頁面都是第一張圖」這件事在任何情況下都成立（包含之後如果其他地方要用到會自動播放的輪播，也不會互相影響）。

改的檔案：`business.html`（創意平面／影像 Photography 改回 `.album-carousel` 排版）、`_layouts/default.html`（輪播共用 JS，修正自動播放判斷邏輯）。

## 3. AI自動化：社群 icon 取代「立即查看」位置，文字改成「點擊icon查看」

「孝子貓」相簿原本「立即查看 →」的連結按鈕已經拿掉，改成在社群圖示前面加一行提示文字「點擊 icon 查看」，圖示本身維持上一輪做的 IG／YT／TikTok／FB／Threads（各自可設連結、留空不顯示）。

改的檔案：`business.html`、`admin/preview.js`（後台預覽同步更新）、`assets/main.css`（新增 `.social-hint` 樣式）。

## 4. Minibar 相簿字級再縮小 2pt、加上 📸 圖案

- 「關於EN／關於日常／關於愛」三個主項目底下的相簿名稱，字級從 11px 再縮小到 9px。
- 每個相簿名稱前面原本是一個小小的「—」記號，這次拿掉，改成 📸 相機圖案，例如「📸 台北街拍」。

改的檔案：`assets/main.css`（`.menu-album-item` 字級、拿掉 `::before` 的「—」樣式）、`_includes/nav.html`（三個主項目的相簿清單都加上 📸）。

## 5. 首頁「查看更多商業作品」按鈕跟黑線靠太近

按鈕跟下面 footer 的分隔線之間，重新加大間距：按鈕外層的上下留白從原本 10px 上邊距，改成上 32px、下 8px，同時「首頁商業精選」整個區塊也加了 56px 的底部留白，確保不管畫面寬度怎麼變，按鈕都不會貼著黑線。

改的檔案：`index.html`（按鈕外層留白）、`assets/main.css`（`#recent-shoots` 新增底部留白）。

---

---

# 第三輪追加：關於商業頁「圖片跑出速度很慢」

排查後，原因不是破圖那種設定錯誤，而是**這一頁一次要載入的照片實在太多、每張檔案也不小**：

- 光是「創意平面」＋「影像 Photography」兩個分類，目前一次會排進頁面的照片就有 40 張以上，每張大概 130KB～550KB（用 GitHub 直接查了幾張實際檔案大小），全部加起來單頁圖片總重量隨便都有 10MB 以上。
- 部分照片是 PNG 格式（例如 `IMG_9491.png`、`1.png`、`蔥玉拌飯-10801350.png` 這幾張）。PNG 是無損壓縮，拿來存「有漸層、有豐富色彩」的相機照片，檔案通常會比存成同品質的 JPEG 大上好幾倍——如果這些不是需要透明背景的設計稿，改存 JPEG 可以省下不少流量。
- 相簿雖然是「輪播」，一次只顯示一張，但**前一版的做法是：不管有沒有被看到，相簿裡的每一張照片都會直接寫進 `<img src="...">`，瀏覽器很容易提前把還沒被看到的照片也一起抓下來**，等於每個相簿都在偷偷多下載好幾張使用者根本還沒看到的照片。

這次先做了「不需要動到照片本身」也能生效的修正：

- 相簿輪播改成**只有第一張照片會馬上載入**，第二張以後的照片先只存网址（`data-src`），要滑到那一張的時候才會真的去下載；往後滑的時候也會順便偷偷預載下一張，切換時不會有空白閃爍，但完全沒被點開的相簿照片就真的不會佔頻寬。這個改動可以讓大部分訪客實際下載到的照片量大幅減少（很多人根本不會把每個相簿都滑完）。

改的檔案：`business.html`（相簿輪播的 `<img>` 改成 `data-src` 延後載入）、`_layouts/default.html`（輪播 JS 新增 `loadSlideImage()` 換圖邏輯）。

**這件事沒辦法完全解決，還需要你之後找時間處理的部分**（我這邊沒有實際圖檔的存取權限，沒辦法直接幫你重新壓縮／轉檔既有照片）：

1. 把「創意平面」相簿裡那幾張 PNG 格式的照片（`IMG_9491.png`、`IMG_9490.png`、`1.png`～`4.png`、`蔥玉拌飯-10801350.png`、幾張「日日敘 Seamless Carousel」）改存成 JPEG，畫質抓 80～85 即可，通常一張可以從幾百 KB 降到 100 KB 上下。
2. 如果你的相機原始檔名照片（`R0008965.JPG`、`R0001533.JPG` 這種）是在 `compress-images.js` 這條自動壓縮流程建立「之前」就上傳的舊照片，代表它們可能從來沒被自動壓縮過。如果你想要，我可以幫你寫一個「一次性」的批次重新壓縮腳本（用 GitHub Actions 手動觸發，掃過 `images/` 資料夾裡所有超過某個檔案大小的照片，統一轉成 JPEG q82、最長邊 1600px），這樣就不用一張一張手動處理；你確認要做的話跟我說一聲即可。

## 這次沒解決、還在等你回覆的事

第二輪提過的問題已經有答案：目前 `en-chiang-site-v2` 跟 `enchiang.com` 是不同的環境，等 V2 全部改到滿意才會轉網域——這件事已經記下來，之後 `baseurl` 就先固定用 `"/en-chiang-site-v2"` 測試，等要轉正式站那天再改回空字串 `""`。

---

# 第三輪再追加：轉網域前，讓「照片跑很慢」不會再發生

你問「如果轉正式網域，要怎麼避免再遇到照片跑很慢」，做了一件可以直接預防的事，加上兩件建議：

## 已經做的事：整批上傳跟後台單張上傳，以後都會自動壓縮

之前「整批上傳 zip」的功能，說明裡就講明白只有「解壓縮＋歸位」，**故意沒有做壓縮**，這就是問題的源頭之一——只要往後持續用這個功能上傳原始照片，遲早會再變慢。這次把壓縮補上：

- 以後不管是透過「整批上傳 zip」，還是直接在後台單張選圖上傳，只要照片進到 `images/` 資料夾，GitHub Actions 都會自動：沒有透明背景需求的 PNG 轉存成 JPEG、任何一邊超過 1600px 的照片等比例縮小、JPEG 統一壓縮到品質 82。已經處理過、本來就夠小的照片不會被重複壓縮。
- 這樣「轉正式站之後才上傳的新照片」就不會再發生這次的問題，是長期有效的做法。

改的檔案：`scripts/process-incoming-zips.js`（新增自動壓縮／轉檔邏輯）、`.github/workflows/incoming-zips.yml`（改成單張上傳也會觸發、加裝壓縮套件 sharp）。

## 還需要你自己決定的兩件事（不是程式碼能直接解決）

1. **既有、還沒被這套新機制處理過的舊照片**：這次的自動化只會處理「之後新增」的照片，repo 裡已經存在的舊照片不會自動回頭處理。想要的話，到 GitHub 網頁的 Actions 分頁手動點一次「Run workflow」，就會把 `images/` 資料夾裡所有現有照片都檢查一次、該轉檔該壓縮的都處理掉——建議轉正式網域前跑一次。
2. **如果想要更進一步（非必要，但轉正式站後如果流量變大會更有感）**：把 `enchiang.com` 這個網域接到 Cloudflare（免費方案就有），開啟它的圖片壓縮／快取功能，等於在你的照片跟訪客之間多一層 CDN 優化，尤其對台灣以外的訪客或行動網路使用者幫助明顯。這個工程量比較大，之後真的要做再跟我說。

## 之後的檔案交付方式，已經照你說的調整

之後每一輪只會給你這次實際改到、新增的檔案，不會再整包壓成一個 zip——這樣你可以一個一個對照著換掉就好，不用先解壓縮再自己比對哪些檔案動過。

---

# 第三輪再追加二：一次性把 V1 的照片搬進 V2 並壓縮

你提的方案：把 V1（正式站 enchiang.com）的照片抓進 V2、清空 V2 現有圖檔、全部重新用壓縮過的版本上傳。方向完全對，但**清空這一步改掉了**，原因是 V2 自己的 `images/` 資料夾裡，已經有透過「整批上傳 zip」進來的照片（例如 `1757425153000_R0009960.JPG`），這幾張 V1 根本沒有——如果先清空再全部從 V1 重抓，這幾張 V2 獨有的照片會直接消失、救不回來。所以改成「只處理還在借用 V1 網址的照片，V2 自己已經有的完全不動」，效果一樣（轉正式站後 V2 不再依賴 V1 的網址），但不會有誤刪風險。

新增了一個**一次性、需要手動觸發**的流程：

- 到 GitHub 網頁的 Actions 分頁，選「一次性搬遷：把 V1 的照片抓進 V2 並壓縮」，點「Run workflow」。
- 它會自動掃過所有資料檔（首頁、四個商業分類、關於EN／日常／愛的文章），找出還寫著 `https://enchiang.com/images/...` 這種網址的照片，一張一張下載下來，套用跟整批上傳一模一樣的壓縮規則（PNG 轉 JPEG、最長邊縮到 1600px、JPEG 壓到品質 82），存進 V2 自己的 `images/` 資料夾，然後把資料檔裡的網址自動改成 V2 本機路徑 `/images/xxx`。
- 檔名裡有空白、中文、全形符號的照片（例如「四款招牌 一起上街.png」、「日日敘 5_4 Seamless Carousel｜01.png」）也都有處理，這是我在自己的測試環境裡實際模擬下載＋壓縮＋替換網址跑過確認沒問題的，不是憑空寫的。
- 這個流程可以重複執行：已經搬過的（網址已經變成本機路徑）不會重複處理；如果中途某張下載失敗，資料檔會保留原本的 V1 網址（不影響前台顯示），下次重跑會再抓一次。
- 這是一次性工具，不會自動觸發，避免每次隨便一個 commit 就意外整批重跑。

改的檔案：新增 `scripts/migrate-v1-images.js`、新增 `scripts/lib/image-optimizer.js`（把壓縮邏輯抽成共用模組，跟整批上傳共用同一套規則，以後只要改一個地方）、`scripts/process-incoming-zips.js`（改成呼叫共用模組，行為不變）、新增 `.github/workflows/migrate-v1-images.yml`。

**建議的執行順序**：先確認這批檔案都已經換上 GitHub，再去 Actions 分頁手動跑一次這個「一次性搬遷」流程，跑完之後花幾分鐘檢查一下 `/business/` 整頁還有各篇文章的相簿，照片都正常顯示、沒有破圖，再繼續下一步。

---

# 第四輪：實際連上你的測試網址抓到兩個 bug 的真正原因，加上 SEO 盤點跟轉移清單

這輪不是用猜的，是直接連到你的測試網址跟正式站的原始碼、CSS、sitemap.xml、robots.txt 逐一檢查出來的。

## 轉盤照片忽大忽小——抓到真正原因了

不是格線本身的問題：照片張數多的相簿（例如婚禮 8 張、燒肉天皇 9 張），瀏覽器算格線兩欄寬度時會被這些相簿裡「還沒捲動到、但技術上還在」的照片撐開最小寬度需求，導致兩欄沒有真的一樣寬。也發現上一輪修正其實沒有真的生效（你上傳的 `main.css` 內容其實還是舊版），這次直接在你的正式測試網址上即時套用新寫法實測過，兩個相簿寬度從 276px／871px（差 3 倍多）變成兩個都精準等於 335.6px，確認有效才寫進這次的 `assets/main.css`。

## 首頁引言文字不斷行——也抓到真正原因了

直接檢查你測試網址目前的原始碼，發現線上那段描述文字目前是整段包在同一個區塊裡，沒有照後台打的樣子一行一行分開，所以瀏覽器把換行當空白處理。這代表上一輪的 `index.html` 修正同樣沒有真的更新上去。這次附上的是正確版本。

## SEO 盤點：V1 用了哪些工具、轉移會不會受影響

完整核對結果（工具是否都有保留）已經存進「ED」這個 Project 的 `V1轉V2正式轉移執行清單.md`，這裡只記重點：GA4 追蹤 ID、AOS 動畫、Lenis 平滑滾動、Google 字體、分享縮圖卡片，V2 都已經保留一致；`robots.txt`（含開放 AI 搜尋引擎爬取）跟 JSON-LD 結構化資料，V2 原本沒有，待補。

真正會影響 SEO 的地方：V1 現在被 Google 收錄 9 個網址，其中 `portrait.html`／`band.html`／`pet.html`／`archive.html`／`business-portrait.html`／`business-brand.html`／`business-video.html` 這 7 個，V2 原本沒有對應頁面，直接覆蓋上去會變成 404、流失排名。

## 新增：4 個分類總覽頁＋3 個轉址頁，讓上面 7 個網址都不會變成死連結

- 新增 `portrait.html`（關於 EN）、`band.html`（關於日常）、`pet.html`（關於愛）——分別對應 `about_en`／`about_daily`／`about_love` 這幾個 collection，會自動列出你之後在後台新增的文章，現在還沒有文章的分類會顯示「相簿陸續整理上架中」（跟 V1 現在的狀態一樣），不用另外維護。
- 新增 `archive.html`（所有文章）——把上面 3 個分類＋關於商業的 3 個子分類，整合成一個總覽頁，對應 V1 原本的「所有文章」頁面。
- 新增 3 個轉址頁 `business-portrait.html`（→ 關於商業｜人像）、`business-brand.html`（→ 關於商業｜創意平面）、`business-video.html`（→ 關於商業｜影片）——V1 這 3 個原本是獨立頁面，V2 已經合併進 `/business/` 這一頁用錨點區分，這 3 個轉址頁讓舊網址、Google 收錄的舊連結、別人分享出去的舊連結，點進來會自動跳轉到新位置，不會看到 404，也用 `rel="canonical"` 盡量把排名價值轉移過去。
- 同時在 `assets/main.css` 補上這幾個新頁面共用的列表樣式。

改的檔案：新增 `portrait.html`、`band.html`、`pet.html`、`archive.html`、`business-portrait.html`、`business-brand.html`、`business-video.html`，更新 `assets/main.css`。

---

# 第四輪再追加：business-*.html 從轉址頁改成真的獨立頁＋後台可編輯

你說不要只做轉址，`business-portrait.html`／`business-brand.html`／`business-video.html` 也要比照 `portrait.html` 這幾頁，做成像 V1 一樣的獨立頁，而且要能在後台編輯文字、換照片。

這 3 個現在都改成真的頁面了，各自完整顯示一個商業分類的相簿／影片內容：

- `business-portrait.html` → 顯示「影像 Photography」（人像）分類
- `business-brand.html` → 顯示「創意平面 Creative Graphics」分類
- `business-video.html` → 顯示「影音 Video」分類

**後台編輯的部分不用另外設定**——這 3 頁直接讀取後台「關於商業」裡原本就有的 3 份資料（① 創意平面、③ 影像、④ 影音），這幾份資料本來就可以在後台編輯文字、新增／更換照片跟影片。也就是說，你現在在後台改「創意平面」的相簿，`/business/` 那一頁的「創意平面」區塊、跟這次新增的 `business-brand.html` 這頁，會同時一起更新，不用改兩次。

`portrait.html`／`band.html`／`pet.html` 這 3 頁也順手補強：最上面的簡介文字（標題、標語、一段描述），改成直接讀取後台首頁設定裡「01／02／03」那三張卡片本來就有的欄位（標題、標語、描述），一樣是後台可以直接編輯的內容，不是寫死在程式碼裡。

`archive.html`「關於商業」那一小節的連結，也跟著改成連去這 3 個新的獨立頁，不再是連去 `/business/` 的錨點。

新增了 `robots.txt`（照你說的，原封不動搬過去，包含開放 GPTBot／ClaudeBot／PerplexityBot 等 AI 搜尋引擎爬取、sitemap 宣告）。

改的檔案：`business-portrait.html`、`business-brand.html`、`business-video.html`（從轉址頁改寫成完整頁面）、`portrait.html`、`band.html`、`pet.html`（簡介文字改成讀後台資料）、`archive.html`（連結更新）、`assets/main.css`（小幅調整）、新增 `robots.txt`。

---

# 第四輪再追加二：補上 JSON-LD 結構化資料

確認過內容之後，加進 `_layouts/default.html`，全站每一頁都會自動帶上這段資料，不用每頁分別設定。內容跟 V1 一致（攝影師身份、服務地區、服務項目），但 IG 連結、Email 改成直接讀後台「聯繫方式」的既有欄位，之後在後台改這兩個資訊會自動同步，不用再麻煩我改程式碼。服務項目除了原本的人像寫真、樂團演出紀錄、寵物到府攝影，這次也照你說的一併加入商業服務（創意平面、商業人像、商業影片），對應這輪新增的 3 個獨立頁。

改的檔案：`_layouts/default.html`。

至此，V1→V2 正式轉移執行清單裡列出的所有待辦項目（4 個分類頁、3 個商業獨立頁、robots.txt、JSON-LD）已經全部完成。
